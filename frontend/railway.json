{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && npm install && npm run build:frontend"
  },
  "deploy": {
    "startCommand": "cd .. && npm run start:frontend"
  }
}
